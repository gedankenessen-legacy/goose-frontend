export interface issueDashboard{
    name: String,
    state: String,
    project: String,
    autho: String,
    progress: number,
    expectedTime: number,
    startDate: Date,
    deadline: Date,
    priority: number 
}
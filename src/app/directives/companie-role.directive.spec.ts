import { CompanieRoleDirective } from './companie-role.directive';

describe('CompanieRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new CompanieRoleDirective();
    expect(directive).toBeTruthy();
  });
});

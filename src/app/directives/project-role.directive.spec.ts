import { ProjectRoleDirective } from './project-role.directive';

describe('ProjectRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new ProjectRoleDirective();
    expect(directive).toBeTruthy();
  });
});
